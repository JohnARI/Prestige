{% extends 'base.html.twig' %}

{% block title %}Modifier l'adresse{% endblock %}

{% block body %}
    <h1>Modifier l'adresse</h1>

    <div class="login_register_wrap section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-6 col-md-10">
                    <div class="login_wrap">
                        <div class="padding_eight_all bg-white">
                            <div class="heading_s1">
                                <h3>Modifier l'adresse</h3>
                            </div>
                            <div class="content">
                                {{ include('adresse/_form.html.twig', {'button_label': 'Update'}) }}
                                <a class="btn btn-border-fill btn-block" href="{{ path('compte') }}">Retour</a>
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
