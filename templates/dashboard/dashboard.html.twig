{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}

<style>
    .btn {
        margin-top: 100px;
    }

    body {
        background-color: darkgray;
    }
</style>
<div class="container">
    <a class="btn btn-primary" href="{{path('showUser')}}">Utilisateurs</a>
    <a class="btn btn-primary" href="{{path('showProduct')}}">Produits</a>
    <a class="btn btn-primary" href="{{path('showCategory')}}">Catégories</a>

</div>


{% endblock %}