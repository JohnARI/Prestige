{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}

<style>

    body {
        background-color: black;
    }

    h2 {
        padding-top: 200px;
    }

</style>

<div class="container vh-100">
<h2 class="text-center text-white">Administrateur</h2>
<br>
<br>
<br>
<br>
<br>
    <div class="text-center">
        <a href="{{path('showUser')}}" class="btn btn-outline-light btn-lg">Gestion des Membres</a>
        <a href="{{path('showProduct')}}" class="btn btn-outline-light btn-lg">Gestion des Produits</a>
        <a href="{{path('showCategory')}}" class="btn btn-outline-light btn-lg">Gestion des Catégories</a> 
        <a href="{{path('showTransporteur')}}" class="btn btn-outline-light btn-lg">Gestion des Transporteurs</a> 

    </div>
</div>

{% endblock %}