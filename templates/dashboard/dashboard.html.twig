{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}

<style>

    body {
        background-color: grey;
    }

    h2 {
        padding-top: 100px;
    }

</style>

<div class="container">
<h2 class="text-center">Administrateur</h2>
    <div class="text-center">
        <a href="{{path('showUser')}}" class="btn btn-outline-dark btn-lg">Gestion des membres</a>
        <a href="{{path('showProduct')}}" class="btn btn-outline-dark btn-lg">Gestion des produits</a>
        <a href="{{path('showCategory')}}" class="btn btn-outline-dark btn-lg">Gestion des catégories</a> 
        <a href="{{path('showTransporteur')}}" class="btn btn-outline-dark btn-lg">Gestion des Transporteurs</a> 

    </div>
</div>

{% endblock %}